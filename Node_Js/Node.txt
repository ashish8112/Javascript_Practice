We don't need to send or upload entire node_modules which is very heavy in size .
We just need to send package.json file along with our programs.

Where all dependecies or library used in my program are written . 
other user or third party just need to run command -> npm install . 

It will download all required libraries in there system .

//randomeuser.me for fetching random api

//blob -